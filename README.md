# 💰 Сайт доната с YooMoney

Простой и красивый сайт для приема донатов через YooMoney с автоматической проверкой платежей.

## ✨ Возможности

- 💳 Прием донатов через YooMoney
- 🔄 Автоматическая проверка платежей через API
- 💵 Автоматический расчет комиссии 3%
- 📊 Статистика по донатам
- ⚙️ Панель администратора
- 🎨 Красивый адаптивный дизайн
- 💾 SQLite база данных
- ✅ Валидация входных данных

## 🚀 Установка

1. Клонируй репозиторий:
```bash
git clone git@github.com:Maybeoff/donat.git
cd donat
```

2. Установи зависимости:
```bash
npm install
```

3. Создай файл `.env` и заполни данные:
```env
YOOMONEY_CLIENT_ID=твой_client_id
YOOMONEY_CLIENT_SECRET=твой_client_secret
YOOMONEY_REDIRECT_URI=http://localhost:3000/oauth/callback
YOOMONEY_RECEIVER=твой_номер_счета
PORT=3000
```

4. Зарегистрируй приложение на https://yoomoney.ru/myservices/new
   - Укажи Redirect URI: `http://localhost:3000/oauth/callback`
   - Получи `client_id` и `client_secret`

5. Запусти сервер:
```bash
npm start
```

6. Открой http://localhost:3000

### 🎨 Vite версия (React + Tailwind)

Для использования современной версии с React и Tailwind CSS:

```bash
# Переключиться на vite бранч
git checkout vite

# Установить зависимости
npm install

# Запустить сервер
npm start
```

Vite версия доступна на: https://github.com/Maybeoff/donat/tree/vite

## 🔐 Авторизация

1. На странице Настроек (`/settings.html`) нажми кнопку "Получить новый токен"
2. Разреши доступ к аккаунту YooMoney
3. Токен сохранится в базе данных автоматически

## 📝 Использование

### Главная страница
- Форма для создания доната с валидацией
- Автоматический расчет суммы с комиссией 3%
- Информация о счете YooMoney
- Список последних донатов с автообновлением

### Страница настроек `/settings.html`
- Управление токеном авторизации
- Статистика по донатам
- Просмотр всех платежей с автообновлением
- Удаление конкретных платежей
- Очистка всех данных

## 🛠️ Технологии

- **Backend**: Node.js, Express
- **Database**: SQLite (better-sqlite3)
- **API**: YooMoney OAuth 2.0
- **Validation**: express-validator
- **Frontend**: Vanilla JS, CSS3

## 📦 Структура проекта

```
donat/
├── public/
│   ├── index.html      # Главная страница
│   ├── settings.html   # Страница настроек
│   ├── script.js       # Скрипты главной страницы
│   ├── settings.js     # Скрипты настроек
│   └── style.css       # Стили
├── db.js              # База данных SQLite
├── server.js          # Сервер Express
├── package.json
├── donat.db           # База данных (создается автоматически)
├── .env              # Конфигурация (не в git)
└── .gitignore
```

## 💾 База данных

Проект использует SQLite с таблицами:
- `payments` - все донаты с индексами
- `settings` - конфигурация (токены и т.д.)

База создается автоматически при первом запуске.

## ✅ Валидация

- Сумма доната: от 1 до 100,000 рублей
- Сообщение: максимум 500 символов
- HTML экранирование для безопасности

## ⚠️ Важно

- Токен YooMoney хранится в базе данных
- Все донаты сохраняются навсегда в SQLite
- База данных исключена из git (.gitignore)

## 📄 Лицензия

MIT
